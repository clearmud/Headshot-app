export interface Filter {
    id: string;
    name: string;
    description: string;
    prompt: string;
}

export interface Background {
    id: string;
    name: string;
    promptFragment: string;
}
